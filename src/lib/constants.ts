// ============================================
// Application Constants
// ============================================

// Create smash wizard
export const TOTAL_CREATE_STEPS = 8;

// File upload limits
export const MAX_PROOF_FILE_SIZE_MB = 50;
export const MAX_PROOF_FILE_SIZE_BYTES = MAX_PROOF_FILE_SIZE_MB * 1024 * 1024;

// Blockchain
export const BYTES32_HEX_LENGTH = 64;

// Price conversion (temporary until oracle integration)
export const ETH_USD_FALLBACK_PRICE = 2500;

// Smash defaults
export const DEFAULT_MAX_PARTICIPANTS = 100;
